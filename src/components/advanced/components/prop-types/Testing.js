import React from "react";
import PropTypes from "prop-types";

const Testing = (props) => {
  return <div></div>;
};

Testing.propTypes = {
  name: PropTypes.array.isRequired, // ptar
  string: PropTypes.string.isRequired, // ptsr
};

export default Testing;
// type rafcp : for shortcut
